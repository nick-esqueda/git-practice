bruh

cloned from GH
created bruh.txt
added this text to bruh.txt

created and switched to 'new-branch'

switched to 'main'
currently commiting and pushing

finished pushing
switched to 'new-branch'
can't switch back to 'main' since bruh.txt is untracked on 'new-branch'
currently commiting and pushing

finished pushing
can't switch back to 'main'. changes need to be committed before switching.
currently committing and pushing

final commit and push before attempting merge 'main' with 'new-branch'

opened pull request, fixed the merge conflict by commiting the fixes to
'new-branch', merged pull request (merged 'main' with 'new-branch'),
deleted 'new-branch', then used git command 'git pull origin main'
to update local main branch with the GH repo's main branch

currently commiting and pushing this text ^ back to origin main.
